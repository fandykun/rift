# Rift Web Dashboard

React + TypeScript frontend for Rift's embedded dashboard.

The visual system is defined in the root `DESIGN.md`. Keep Tailwind tokens in `tailwind.config.ts` aligned with that document.
